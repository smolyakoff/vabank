<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003" DefaultTargets="Build">
  <Import Project="$(MSBuildProjectFullPath)\VaBank.proj"></Import>
  
  
  <Target Name="Build">

  </Target>

  <Target Name="Migrate">
    <Message Text="Running code based migrations..." Importance="High" />
  </Target>
  

</Project>